<%= form_for @card do |c| %> 
<!-- 
	<%= c.label :deck %>
	<%= c.text_field :deck %> -->

	<%= c.label :question %> 
	<%= c.text_field :question %> 

	<%= c.label :answer %>
	<%= c.text_field :answer %>

	<%= c.submit %>

<% end %>